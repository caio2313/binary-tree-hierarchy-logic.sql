# binary-tree-hierarchy-logic.sql
Hierarchical Relations, Non-Equi Subqueries, and Self-Referencing Logic.  
Challenge Description: Given a table containing binary tree nodes with an ID column (N) and a parent ID column (P), the task is to write a query that dynamically determines the structural type of each node.
